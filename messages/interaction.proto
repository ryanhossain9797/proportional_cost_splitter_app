syntax = "proto3";
package interaction;

import "schemas.proto";

message RustCalculateRequest {
  repeated schemas.RustCostEntry initial_costs = 1;
  float final_total_cost = 2;
}

message RustCalculateResponse {
  repeated schemas.RustFinalCost final_costs = 1;
}